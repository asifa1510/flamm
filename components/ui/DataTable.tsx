'use client';
import { useState } from 'react'; import type { DataPoint } from '@/lib/types';
export function DataTable({data}:{data:DataPoint[]}){const [top,setTop]=useState(0),row=34,view=238,start=Math.floor(top/row),visible=data.slice(start,start+Math.ceil(view/row)+2);return <div className="telemetry-table"><div className="table-head"><span>ID</span><span>TIME</span><span>VALUE</span><span>SIGNAL</span></div><div className="table-scroll" onScroll={e=>setTop(e.currentTarget.scrollTop)}><div style={{height:data.length*row,position:'relative'}}><div style={{position:'absolute',top:start*row,width:'100%'}}>{visible.map(p=><div className="table-row" key={p.id}><span>#{p.id}</span><span>{new Date(p.timestamp).toLocaleTimeString([], {hour12:false})}</span><span>{p.value.toFixed(2)}</span><span>{(p.signal*100).toFixed(1)}%</span></div>)}</div></div></div></div>}
